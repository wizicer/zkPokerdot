{
  "name": "circuits",
  "scripts": {
    "setup:ptau": "bash ./scripts/gentesttau.sh",
    "build:decrypt": "bash ./scripts/compile.sh src/decrypt",
    "build:shuffle": "bash ./scripts/compile.sh src/shuffle_encrypt",
    "lint:fix": "npm run lint:fix --if-present",
    "lint:check": "npm run lint:check --if-present",
    "prettier:fix": "npm run prettier:fix --if-present",
    "prettier:check": "npm run prettier:check --if-present"
  },
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.0",
    "@types/node": "^18.11.9",
    "chai": "^4.3.7",
    "dotenv": "^16.0.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "dotenv": "^16.0.3"
  }
}
